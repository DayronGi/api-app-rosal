{{-- @extends('layouts.plantillas')

@section('title', 'Task' . $task->worker_id)

@section('content')
    <h1>Bienvenido al curso {{$task->worker_id}}</h1>
    <a href="{{route('tasks.list')}}">Volver a cursos</a>
    <br>
    <a href="{{route('tasks.edit', $task->task_id)}}">Editar curso</a>
    <p><strong>Categoria: </strong>{{$task->job_id}}</p>
    <p>{{$task->description}}</p>
@endsection --}}
